#include <stdio.h>
int factrec(int n);
int main()
{
    int n;
    scanf("%d",&n);
    printf("%d",factrec(n));

    return 0;
}
int factrec(int n){
    if 
    (n>=1)
    return n*factrec(n-1);
    else
    return 1;
}
