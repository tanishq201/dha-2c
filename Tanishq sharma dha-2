#include<stdio.h>
void sieve(int N)
{   int i;
int prime[N];
for(i=2;i<=N;i++)
    prime[i]=1;
for(i=2;i*i<=N;i++)
{
    if(prime[i]==1)
    {
        for(int j=i;i*j<=N;j++)
            prime[i*j]=0;
     }
}
for(i=2;i<N;i++)
{
    if(prime[i]!=0)
        printf("%d ",i);
}
}
int main()
{
const int n;
scanf("%d",&n);
sieve(n);
return 0;
}
